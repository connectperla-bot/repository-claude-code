# Copie di lavoro dei file del tema

**La fonte di verità è Shopify**, non questa cartella: il tema in bozza
`196807885144` è dove i file vivono davvero, e Shopify ne tiene le versioni.

Qui stanno solo le copie che questa sessione ha modificato e non è ancora
riuscita a ricaricare. Esistono per un motivo pratico: il contenitore di lavoro
si è riavviato sette volte in una giornata, e ogni volta si è portato via i file
non committati — due volte proprio a metà di questa correzione.

Nomi appiattiti (`sections-`, `snippets-`, `assets-`) perché la struttura del
tema non c'entra con quella di questo repository e non deve sembrare che lo sia.

Quando i file sono su Shopify e verificati con l'MD5, questa cartella si può
cancellare.
